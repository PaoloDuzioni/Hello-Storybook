import { Meta, Story, Canvas, Props } from '@storybook/addon-docs/blocks';
import ButtonGroup from './';
import Button from '../../input/Button/';

<Meta title="LAYOUT/ButtonGroup" component={ButtonGroup} />

# Button Group

This is the `ButtonGroup` component in it's default state. Buttons aligned to the left:

<Canvas>
    <Story name="Default" argTypes={{
        justify: {
            control: {
                type: 'inline-radio',
                options: ['flex-start', 'center', 'flex-end', 'space-between', 'space-around', 'space-evenly'],
            }
        }
    }}>
       {args =>
       <ButtonGroup {...args}>
            <Button>
                Default button
            </Button>
            <Button primary>
                Primary button
            </Button>
            <Button secondary>
                Secondary button
            </Button>
            <Button disabled>
                Disabled button
            </Button>
            <Button as="a" href="#" primary>
                Link button
            </Button>
        </ButtonGroup>
        }
    </Story>
</Canvas>

## Component Props

<Props of={ButtonGroup} />

## Some Examples

### Centered

With the `justify` property with value of `center`:

<Canvas>
    <Story name="Centered">
       <ButtonGroup justify="center">
            <Button>
                Default button
            </Button>
            <Button primary>
                Primary button
            </Button>
            <Button secondary>
                Secondary button
            </Button>
            <Button disabled>
                Disabled button
            </Button>
            <Button as="a" href="#" primary>
                Link button
            </Button>
        </ButtonGroup>
    </Story>
</Canvas>

### Right

With the `justify` property with value of `flex-end`:

<Canvas>
    <Story name="Right">
       <ButtonGroup justify="flex-end">
            <Button>
                Default button
            </Button>
            <Button primary>
                Primary button
            </Button>
            <Button secondary>
                Secondary button
            </Button>
            <Button disabled>
                Disabled button
            </Button>
            <Button as="a" href="#" primary>
                Link button
            </Button>
        </ButtonGroup>
    </Story>
</Canvas>

### Space between

With the `justify` property with value of `space-between`:

<Canvas>
    <Story name="Space Between">
       <ButtonGroup justify="space-between">
            <Button>
                Default button
            </Button>
            <Button primary>
                Primary button
            </Button>
            <Button secondary>
                Secondary button
            </Button>
            <Button disabled>
                Disabled button
            </Button>
            <Button as="a" href="#" primary>
                Link button
            </Button>
        </ButtonGroup>
    </Story>
</Canvas>